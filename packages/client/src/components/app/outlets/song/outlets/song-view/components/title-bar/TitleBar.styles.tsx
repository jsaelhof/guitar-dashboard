import { styled } from "@mui/material";

export const Layout = styled("div")({
  display: "grid",
  gridTemplateColumns: "1fr auto auto",
  alignItems: "stretch",
  gap: 16,
  marginBottom: 16,
  marginLeft: 4,
});
